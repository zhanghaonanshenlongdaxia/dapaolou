# 打炮楼游戏 - 完整开发手册

> 📅 创建时间：2024年
> 🎮 游戏类型：3D物理弹珠对战
> 🔧 引擎：Unity 2022.3 LTS + QFramework

---

## 🎯 快速概览

### 游戏简介
**打炮楼**是一款重现童年回忆的3D物理弹珠对战游戏。三个兄弟在院子里打弹珠，同时要应对妈妈派活、宠物捣乱等各种干扰。

### 核心角色
| 角色 | 类型 | 说明 |
|------|------|------|
| **大哥** | 玩家可选 | 均衡型，稳定瞄准 |
| **二弟** | 玩家可选 | 力量型，高暴击 |
| **三弟** | 玩家可选 | 技巧型，双发弹珠 |
| **妈妈** | NPC | 做饭、喊吃饭、派活、踢人 |
| **牛** | NPC | 需要喝水、牵回牛圈 |
| **臭豆** | 宠物狗 | 偶尔跑出来看玩家玩耍 |
| **骚咪** | 宠物猫 | 会骚扰弹珠 |

### 核心系统
1. **弹珠物理系统** - 高尔夫式弹射 + 荒野召唤式手抖
2. **角色选择系统** - 三个兄弟各有特色，每人21个弹珠
3. **时间系统** - 游戏从早上6:00到晚上20:00
4. **NPC系统** - 妈妈、牛、臭豆、骚咪各有行为
5. **任务系统** - 打水、牵牛、吃饭等任务
6. **地形系统** - 水泥地、泥土地、草堆、松散土地

### 游戏流程
```
早上6:00 开始
    ↓
选择角色（大哥/二弟/三弟）
    ↓
摆炮楼（每人21个弹珠）
    ↓
石头剪刀布决定顺序
    ↓
开始打弹珠（应对各种干扰）
    ↓
晚上8:00 游戏结束
```

---

## 📖 目录

1. [游戏概述](#游戏概述)
2. [核心玩法](#核心玩法)
3. [时间系统](#时间系统)
4. [NPC系统](#npc系统)
5. [项目结构](#项目结构)
6. [系统详解](#系统详解)
7. [Unity设置指南](#unity设置指南)
8. [手抖系统详解](#手抖系统详解)
9. [力度系统详解](#力度系统详解)
10. [地形系统详解](#地形系统详解)
11. [调试技巧](#调试技巧)
12. [扩展功能](#扩展功能)
13. [常见问题](#常见问题)
14. [开发路线图](#开发路线图)

---

## 🎯 游戏概述

### 灵感来源
- 童年打炮楼游戏（玻璃弹珠）
- 《荒野的召唤》手抖系统
- 高尔夫游戏力度控制

### 核心特色
- **3D物理引擎**：真实的弹珠碰撞和滚动
- **手抖系统**：类似《荒野的召唤》的操作手感
- **地形效果**：4种不同地形影响弹珠行为
- **策略玩法**：炮楼结构、二次打爆、拆炮楼机制
- **低多边形风格**：复古童年回忆

---

## 🎮 核心玩法

### 炮楼结构
```
    ○      ← 顶部（1个）
   ○○○     ← 底部（3个，三角形排列）
```

**总弹珠数**：4个

### 小兵系统
- 每个玩家：3个小兵弹珠
- 小兵用途：攻击对方炮楼和小兵
- 位置：炮楼前方扇形分布

### 回合流程
```
1. 选择弹珠（小兵优先）
2. 瞄准（第一人称视角）
3. 蓄力（高尔夫式力度条）
4. 发射（手抖影响方向）
5. 等待弹珠停止
6. 判定碰撞结果
7. 切换到下一个玩家
```

### 攻击规则

#### 弹射机制
- **瞄准**：右键进入瞄准模式
- **蓄力**：左键按住，力度条往返移动
- **发射**：松开左键，弹珠射出
- **手抖**：影响弹珠发射方向

#### 打爆判定
| 情况 | 条件 | 结果 |
|------|------|------|
| 一次打爆 | 力度足够大（>5） | 直接摧毁 |
| 二次打爆 | 炮楼剩1-3个弹珠 | 需要二次攻击 |
| 未打爆 | 力度不足 | 弹珠弹开 |

#### 拆炮楼规则
- **触发条件**：小兵全灭
- **操作**：拆自己炮楼补充小兵
- **限制**：每次只能拆1个
- **策略**：保命还是保炮楼？

### 胜利条件
- 摧毁对方所有炮楼弹珠
- 或对方小兵全灭且无法拆炮楼

---

## ⏰ 时间系统

### 时间流逝
游戏从**早上6:00**开始，到**晚上20:00**结束。

```
时间线：
6:00  ────── 游戏开始
7:00  ────── 妈妈牵牛出门干活
11:30 ────── 妈妈回来做午饭
12:00 ────── 妈妈第1次喊吃饭
12:15 ────── 妈妈第2次喊吃饭
12:30 ────── 妈妈第3次喊吃饭
12:45 ────── 妈妈第4次喊吃饭
12:50 ────── 妈妈踢玩家（强制吃饭）
13:00 ────── 妈妈下午出门干活
15:00 ────── 牛需要喝水
17:30 ────── 妈妈回来做晚饭
18:00 ────── 妈妈第1次喊吃晚饭
18:15 ────── 妈妈第2次喊吃晚饭
18:30 ────── 妈妈第3次喊吃晚饭
18:45 ────── 妈妈第4次喊吃晚饭
18:50 ────── 妈妈踢玩家（强制吃饭）
19:00 ────── 牛需要牵回牛圈
19:30 ────── 天黑
20:00 ────── 游戏结束
```

### 光照变化
- **清晨**：暖色调，光线柔和
- **上午**：明亮，正常光照
- **中午**：略微偏黄
- **下午**：光线逐渐变暗
- **傍晚**：橙红色调
- **晚上**：昏暗，几乎看不清

### 时间配置

```csharp
// TimeSystem配置
timeScale = 60f;        // 1秒 = 游戏内1分钟
startHour = 6;          // 开始时间
endHour = 20;           // 结束时间
```

---

## 👨‍👩‍👧‍👦 NPC系统

### 重要人物

#### 1. 妈妈 👩

**行为时间表**：
| 时间 | 行为 | 说明 |
|------|------|------|
| 7:00 | 牵牛出门 | 妈妈把牛牵到院子外 |
| 7:00-11:30 | 出门干活 | 妈妈不在家 |
| 11:30 | 回来做午饭 | 妈妈回到厨房 |
| 12:00-12:45 | 喙吃饭 | 每15分钟喊一次，共4次 |
| 12:50 | 踢玩家 | 超过4次没去，强制踢玩家 |
| 13:00 | 出门干活 | 妈妈下午继续出门 |
| 17:30 | 回来做晚饭 | 妈妈回到厨房 |
| 18:00-18:45 | 喊吃晚饭 | 每15分钟喊一次，共4次 |
| 18:50 | 踢玩家 | 超过4次没去，强制踢玩家 |
| 19:00 | 让玩家牵牛 | 要求把牛牵回牛圈 |

**交互**：
- 与妈妈对话可以响应吃饭
- 答应去牵牛
- 被踢后游戏暂停，强制吃饭

**代码示例**：
```csharp
// 获取妈妈NPC
MomNPC mom = NPCManager.Instance.GetMom();

// 检查妈妈状态
MomBehavior behavior = mom.GetCurrentBehavior();
if (behavior == MomBehavior.CallingForMeal)
{
    // 响应吃饭
    mom.OnPlayerRespondsToMeal();
}
```

---

#### 2. 牛 🐂

**状态**：
```csharp
public enum CowState
{
    InPen,          // 在牛圈里
    Outside,        // 在院子外
    Drinking,       // 喝水中
    Following,      // 跟随玩家
    Returned        // 已回到牛圈
}
```

**行为流程**：
1. **早上7:00**：妈妈牵牛出门
2. **白天**：牛在院子外，偶尔叫唤
3. **下午15:00**：牛需要喝水（玩家任务）
4. **傍晚19:00**：牛需要牵回牛圈（玩家任务）

**交互**：
- **牵牛**：与牛交互，牛会跟随玩家
- **给牛喝水**：在水桶位置与牛交互
- **牵回牛圈**：把牛带到牛圈位置

**代码示例**：
```csharp
// 获取牛NPC
CowNPC cow = NPCManager.Instance.GetCow();

// 牵牛
cow.StartFollowing(playerTransform);

// 给牛喝水
cow.OnPlayerGivesWater();

// 牵回牛圈
cow.OnPlayerLeadsReturn();
```

---

#### 3. 臭豆（宠物狗）🐕

**状态**：
```csharp
public enum DogState
{
    Tied,           // 拴着
    Wandering,      // 闲逛中
    Watching,       // 看玩家玩耍
    Barking,        // 叫唤中
    Returning       // 返回狗窝
}
```

**行为特点**：
- 拴在院子墙根底下
- 偶尔会逃跑（每分钟10%概率）
- 逃跑后会在院子里闲逛
- 会跑到玩家附近观看玩耍
- 偶尔叫唤
- 一段时间后自动返回狗窝

**交互**：
- **摸狗**：在狗拴着或观看时交互
- **赶狗**：在狗闲逛时交互，狗会跑开
- **抓狗**：狗逃跑后，可以手动赶回

**代码示例**：
```csharp
// 获取狗NPC
DogNPC dog = NPCManager.Instance.GetDog();

// 检查是否逃跑
if (dog.IsEscaped())
{
    Debug.Log("臭豆跑出来了！");
}

// 赶狗
dog.ScareAway();

// 强制返回
dog.ForceReturn();
```

---

#### 4. 弟弟们 👦👦

**弟弟1（臭豆）**：
- 性格：懒惰
- 特点：爱推脱，容易拒绝任务

**弟弟2（骚咪）**：
- 性格：调皮
- 特点：爱捣乱，偶尔推脱

**状态**：
```csharp
public enum BrotherPersonality
{
    Lazy,           // 懒惰（爱推脱）
    Obedient,       // 听话（容易答应）
    Naughty         // 调皮（爱捣乱）
}
```

**行为特点**：
- 会一起打弹珠
- 妈妈派活时会相互推脱
- 被踢后会抱怨
- 参与石头剪刀布决定顺序

**交互**：
- 与弟弟对话
- 一起打弹珠
- 相互推脱任务

---

#### 5. 骚咪（宠物猫）🐱

**状态**：
```csharp
public enum CatState
{
    Idle,           // 空闲
    Wandering,      // 闲逛中
    Lying,          // 躺着
    Sitting,        // 坐着
    Disturbing,     // 骚扰玩家中
    Fleeing         // 逃跑中
}
```

**行为特点**：
- 在院子里闲逛
- 喜欢躺在台阶上或土墙上
- 偶尔会骚扰玩家的弹珠
- 骚扰时会拨弄弹珠，影响游戏
- 被赶走后会逃跑

**交互**：
- **摸猫**：在猫躺着或坐着时交互
- **赶猫**：在猫骚扰时交互，猫会逃跑

**骚扰机制**：
```csharp
// 骚扰配置
disturbRange = 3f;          // 骚扰范围
disturbChance = 0.15f;      // 骚扰概率
disturbDuration = 20f;      // 骚扰持续时间

// 骚扰时会拨弄弹珠
Rigidbody rb = targetMarble.GetComponent<Rigidbody>();
rb.AddForce(pushDirection * 2f, ForceMode.Impulse);
```

**代码示例**：
```csharp
// 获取猫NPC
CatNPC cat = NPCManager.Instance.GetCat();

// 检查是否在骚扰
if (cat.IsDisturbing())
{
    // 赶走猫
    cat.ScareAway();
}
```

---

## 👤 角色选择系统

### 三个兄弟

游戏开始时，玩家可以选择扮演三个兄弟中的一个。每人开局**21个弹珠**。

#### 大哥（你）- 均衡型
```
力度：★★★☆☆ (3-12)
稳定：★★★★★ (90%)
速度：★★★☆☆ (3.0)
特殊：稳定瞄准
```
- **属性**：力度适中，稳定性最高
- **特点**：适合新手，容易控制
- **特殊能力**：可以稳定瞄准（减少手抖）

#### 二弟 - 力量型
```
力度：★★★★★ (5-18)
稳定：★★☆☆☆ (70%)
速度：★★★★☆ (3.5)
特殊：高暴击
```
- **属性**：力度最大，但稳定性差
- **特点**：适合喜欢大力出奇迹的玩家
- **特殊能力**：暴击率20%，暴击伤害1.8倍

#### 三弟 - 技巧型
```
力度：★★☆☆☆ (2-10)
稳定：★★★★☆ (80%)
速度：★★★★★ (4.0)
特殊：双发弹珠
```
- **属性**：力度较小，但可以双发
- **特点**：适合技巧型玩家
- **特殊能力**：可以同时发射2个弹珠

### 属性对比表

| 属性 | 大哥 | 二弟 | 三弟 |
|------|------|------|------|
| 初始弹珠 | 21 | 21 | 21 |
| 最小力度 | 3 | 5 | 2 |
| 最大力度 | 12 | 18 | 10 |
| 蓄力速度 | 1.0x | 0.9x | 1.3x |
| 基础稳定性 | 90% | 70% | 80% |
| 手抖幅度 | 0.015 | 0.03 | 0.02 |
| 瞄准漂移 | 0.3 | 0.7 | 0.5 |
| 行走速度 | 3.0 | 3.5 | 4.0 |
| 暴击率 | 10% | 20% | 15% |
| 特殊能力 | 稳定瞄准 | 高暴击 | 双发 |

### 代码示例

```csharp
// 获取角色管理器
CharacterManager charManager = CharacterManager.Instance;

// 获取所有角色
CharacterConfig[] characters = charManager.GetAllCharacters();

// 选择角色
charManager.SelectCharacter(CharacterType.Eldest);  // 大哥
charManager.SelectCharacter(CharacterType.Second);   // 二弟
charManager.SelectCharacter(CharacterType.Third);    // 三弟

// 获取当前角色
CharacterConfig current = charManager.GetCurrentCharacter();
Debug.Log($"当前角色: {current.characterName}");
Debug.Log($"力度范围: {current.minShootForce}-{current.maxShootForce}");
Debug.Log($"稳定性: {current.baseStability}");

// 应用角色属性到玩家
PlayerManager player = FindObjectOfType<PlayerManager>();
charManager.ApplyCharacterToPlayer(player);

// 获取角色描述
string summary = CharacterPresets.GetCharacterSummary(CharacterType.Eldest);
```

### 弹珠库存系统

每个玩家开局21个弹珠：
- **炮楼**：4个弹珠（3底+1顶）
- **小兵**：3个弹珠
- **备用**：14个弹珠（用于拆炮楼补充）

```csharp
// 获取弹珠库存
MarbleInventory inventory = MarbleInventory.Instance;

// 初始化玩家弹珠（使用角色配置）
CharacterConfig charConfig = CharacterManager.Instance.GetCurrentCharacter();
inventory.InitializeWithCharacter(playerId, charConfig);

// 获取弹珠数量
int total = inventory.GetTotalMarbles(playerId);
int available = inventory.GetAvailableMarbles(playerId);

// 消耗弹珠（被打爆）
inventory.ConsumeMarble(playerId, isTower: true);  // 炮楼被毁
inventory.ConsumeMarble(playerId, isTower: false); // 小兵被毁

// 获得弹珠（缴获）
inventory.AddMarbles(playerId, 3);  // 获得3个弹珠

// 拆炮楼补充小兵
inventory.DismantleTower(playerId);

// 从备用补充小兵
inventory.RecruitSoldier(playerId);

// 检查状态
bool hasTower = inventory.HasTower(playerId);
bool hasSoldiers = inventory.HasSoldiers(playerId);
bool isEliminated = inventory.IsPlayerEliminated(playerId);

// 获取排名
string rankings = inventory.GetRankingText();
```

---

### NPC管理器

**NPCManager** 负责协调所有NPC和时间事件。

```csharp
// 获取NPC管理器
NPCManager npcManager = NPCManager.Instance;

// 获取各个NPC
MomNPC mom = npcManager.GetMom();
CowNPC cow = npcManager.GetCow();
DogNPC dog = npcManager.GetDog();
CatNPC cat = npcManager.GetCat();

// 检查是否可以交互
if (npcManager.CanInteract())
{
    NPCBase nearest = npcManager.GetNearestNPC();
    // 与NPC交互
}

// 获取弟弟
BrotherNPC brother1 = npcManager.GetBrother1();
BrotherNPC brother2 = npcManager.GetBrother2();

// 获取所有玩家名称（包括主角和弟弟）
List<string> playerNames = npcManager.GetAllPlayerNames();
```

---

### 弟弟系统

**弟弟NPC** 是主角的弟弟，会一起打弹珠。

**弟弟1（臭豆）**：
- 性格：懒惰
- 特点：爱推脱，容易拒绝任务

**弟弟2（骚咪）**：
- 性格：调皮
- 特点：爱捣乱，偶尔推脱

**代码示例**：
```csharp
// 获取弟弟
BrotherNPC brother1 = NPCManager.Instance.GetBrother1();
BrotherNPC brother2 = NPCManager.Instance.GetBrother2();

// 检查弟弟状态
if (brother1.IsPlayingMarbles())
{
    Debug.Log("弟弟1在打弹珠");
}

if (brother2.IsDoingChores())
{
    Debug.Log("弟弟2在干活");
}

// 弟弟收到任务
brother1.OnReceiveTask("打水给牛喝");

// 弟弟被踢
brother1.OnKickedByMom();
```

---

### 妈妈派活系统

**派活流程**：
1. 妈妈宣布任务
2. 随机指派一个人
3. 被指派的人可以：
   - 答应干活（30%概率）
   - 推脱（50%概率）
   - 推给别人（40%概率）
4. 如果推脱，指派下一个人
5. 重复3轮
6. 如果所有人都推脱：
   - 所有人挨一脚
   - 强制玩家去干活
   - 游戏暂停

**代码示例**：
```csharp
// 获取妈妈派活系统
MomTaskDelegate taskDelegate = FindObjectOfType<MomTaskDelegate>();

// 妈妈派活
taskDelegate.DelegateTask("打水给牛喝");

// 监听事件
taskDelegate.OnTaskDelegated += (targetIndex) =>
{
    Debug.Log($"任务派给了: {targetIndex}");
};

taskDelegate.OnPlayerRefuses += (playerIndex) =>
{
    Debug.Log($"玩家 {playerIndex} 推脱了");
};

taskDelegate.OnAllRefused += () =>
{
    Debug.Log("所有人都推脱了，妈妈生气了！");
};

taskDelegate.OnPlayerForced += () =>
{
    Debug.Log("玩家被强制去干活");
};

// 玩家响应
taskDelegate.PlayerAgrees(); // 答应
taskDelegate.PlayerRefuses(); // 推脱
```

---

### 石头剪刀布系统

**用途**：决定谁先开始打弹珠。

**流程**：
1. 所有玩家参与
2. 石头剪刀布
3. 获胜者先开始
4. 如果平局，重新来过

**代码示例**：
```csharp
// 获取石头剪刀布系统
RockPaperScissors rps = FindObjectOfType<RockPaperScissors>();

// 开始游戏
List<string> playerNames = new List<string> { "我", "臭豆", "骚咪" };
rps.StartRPS(playerNames);

// 监听事件
rps.OnGameEnd += (winner) =>
{
    Debug.Log($"{winner.playerName}获胜！先开始！");
};

rps.OnRoundResult += (result) =>
{
    Debug.Log($"本局结果: {result}");
};

// 获取获胜者
RPSPlayer winner = rps.GetWinner();
int winnerIndex = rps.GetWinnerIndex();
```

---

### 游戏开始流程

**游戏开始管理器** 负责处理游戏开始前的准备工作。

**阶段**：
1. **介绍阶段**：游戏背景介绍
2. **摆炮楼**：玩家摆好炮楼
3. **石头剪刀布**：决定谁先开始
4. **决定顺序**：确定游戏顺序
5. **游戏开始**：正式开始

**代码示例**：
```csharp
// 获取游戏开始管理器
GameStartManager startManager = FindObjectOfType<GameStartManager>();

// 获取当前阶段
GameStartPhase phase = startManager.GetCurrentPhase();

// 获取游戏顺序
int[] playOrder = startManager.GetPlayOrder();

// 完成摆炮楼
startManager.CompleteSetup();

// 跳过介绍
startManager.SkipIntro();
```

---

### 任务系统

任务系统管理玩家需要完成的各种任务。

**任务类型**：
```csharp
public enum TaskType
{
    FetchWater,         // 打水给牛喝
    LeadCowOut,         // 牵牛出门
    LeadCowIn,          // 牵牛回圈
    EatMeal,            // 吃饭
    ScareCat,           // 赶猫
    CatchDog,           // 抓狗
    PlayMarbles         // 打弹珠（默认）
}
```

**任务优先级**：
| 优先级 | 任务 | 说明 |
|--------|------|------|
| 4 | 吃饭 | 最高优先级，必须完成 |
| 3 | 打水/牵牛 | 重要任务，妈妈会检查 |
| 2 | 一般任务 | 可以延迟 |
| 1 | 赶猫/抓狗 | 可选任务 |

**任务超时后果**：
- **打水超时**：妈妈会踢玩家
- **牵牛超时**：妈妈会踢玩家
- **吃饭超时**：妈妈会踢玩家
- **赶猫超时**：弹珠可能被骚扰
- **抓狗超时**：狗继续捣乱

**代码示例**：
```csharp
// 获取任务系统
TaskSystem taskSystem = TaskSystem.Instance;

// 检查当前任务
TaskData mainTask = taskSystem.GetCurrentMainTask();
if (mainTask != null)
{
    Debug.Log($"当前任务: {mainTask.taskName}");
    Debug.Log($"剩余时间: {taskSystem.GetTaskRemainingTime(mainTask.taskType)}");
}

// 完成任务
taskSystem.CompleteTask(TaskType.FetchWater);
```

---

## 📁 项目结构

### 目录结构
```
Assets/Scripts/
├── Game/                           # 游戏核心逻辑
│   ├── GameManager.cs              # 游戏管理器（单例）
│   ├── GameConfig.cs               # 游戏配置（ScriptableObject）
│   ├── PlayerData.cs               # 玩家数据
│   ├── TowerBuilder.cs             # 炮楼建造器
│   ├── RockPaperScissors.cs        # 石头剪刀布系统
│   ├── GameStartManager.cs         # 游戏开始管理器
│   ├── CharacterConfig.cs          # 角色配置系统
│   ├── CharacterPresets.cs         # 角色预设
│   └── MarbleInventory.cs          # 弹珠库存系统
│
├── Marble/                         # 弹珠系统
│   ├── MarbleData.cs               # 弹珠数据组件
│   ├── MarbleShooter.cs            # 弹珠发射器
│   ├── MarbleCollisionHandler.cs   # 碰撞处理器
│   └── HandTremorSystem.cs         # 🎯 手抖系统
│
├── Player/                         # 玩家系统
│   ├── FirstPersonController.cs    # 第一人称控制器
│   ├── ThirdPersonModel.cs         # 第三人称模型
│   └── PlayerManager.cs            # 玩家管理器
│
├── Terrain/                        # 地形系统
│   └── TerrainEffectSystem.cs      # 地形效果管理
│
├── Time/                           # ⏰ 时间系统
│   └── TimeSystem.cs               # 时间管理和事件触发
│
├── NPC/                            # 👨‍👩‍👧‍👦 NPC系统
│   ├── NPCBase.cs                  # NPC基类
│   ├── MomNPC.cs                   # 妈妈NPC
│   ├── CowNPC.cs                   # 牛NPC
│   ├── DogNPC.cs                   # 宠物狗（臭豆）
│   ├── CatNPC.cs                   # 宠物猫（骚咪）
│   ├── BrotherNPC.cs               # 弟弟NPC
│   ├── MomTaskDelegate.cs          # 妈妈派活系统
│   └── NPCManager.cs               # NPC管理器
│
├── Task/                           # 📋 任务系统
│   └── TaskSystem.cs               # 任务管理
│
├── UI/                             # UI系统
│   ├── GameUIManager.cs            # 游戏UI管理
│   └── CharacterSelectUI.cs        # 角色选择UI
│
├── Editor/                         # 编辑器工具
│   └── QuickSetup.cs               # 快速设置工具
│
└── README.md                       # 本文档
```

### 脚本依赖关系
```
GameManager
    ├── PlayerData
    ├── TowerBuilder
    │   └── MarbleData
    └── MarbleShooter
        ├── MarbleData
        └── HandTremorSystem

PlayerManager
    ├── FirstPersonController
    ├── ThirdPersonModel
    ├── MarbleShooter
    └── HandTremorSystem

MarbleCollisionHandler
    └── GameManager (通知)

TerrainEffectSystem
    └── MarbleData (应用效果)
```

---

## 🔧 系统详解

### 1. 弹珠系统 (MarbleData)

```csharp
// 弹珠类型
public enum MarbleType
{
    Tower,      // 炮楼弹珠
    Soldier     // 小兵弹珠
}

// 弹珠状态
public enum MarbleState
{
    Idle,       // 静止
    Rolling,    // 滚动中
    Destroyed   // 被打爆
}
```

**核心属性**：
- `ownerPlayerId` - 所属玩家
- `marbleType` - 弹珠类型
- `state` - 当前状态
- `isInvincible` - 无敌状态

### 2. 炮楼建造器 (TowerBuilder)

**炮楼结构**：
```
默认配置：
- 底部：3个弹珠（三角形）
- 顶部：1个弹珠
- 间距：弹珠直径 * 1.05
- 物理约束：FixedJoint连接
```

**稳定性设计**：
- 使用FixedJoint连接弹珠
- 设置breakForce防止倒塌
- 碰撞时自动断开连接

### 3. 游戏管理器 (GameManager)

**单例模式**：
```csharp
public static GameManager Instance { get; private set; }
```

**游戏流程**：
```csharp
public enum GamePhase
{
    Setup,      // 设置阶段（等待弹珠静止）
    Playing,    // 游戏进行中
    GameOver    // 游戏结束
}
```

**核心事件**：
```csharp
public System.Action<int> OnPlayerTurnStart;      // 回合开始
public System.Action<int> OnPlayerTurnEnd;         // 回合结束
public System.Action<int> OnGameOver;              // 游戏结束
public System.Action<MarbleData, MarbleData> OnMarbleDestroyed; // 弹珠被摧毁
```

---

## 🎯 手抖系统详解

### 类似《荒野的召唤》的效果

手抖系统模拟真实的手部不稳定，影响弹珠发射方向。

### 影响因素

```csharp
public class TremorFactors
{
    [Range(0f, 1f)] public float baseStability = 0.8f;     // 基础稳定性
    [Range(0f, 1f)] public float nervousness = 0f;          // 紧张程度
    [Range(0f, 1f)] public float fatigue = 0f;              // 疲劳程度
    [Range(0f, 1f)] public float terrainEffect = 0f;        // 地形影响
    [Range(0f, 1f)] public float windEffect = 0f;           // 风力影响
}
```

### 稳定性计算

```csharp
// 最终稳定性 = 基础 - 各种影响
currentStability = baseStability;
currentStability -= nervousness * 0.4f;
currentStability -= fatigue * 0.3f;
currentStability -= terrainEffect * 0.2f;
currentStability -= windEffect * 0.15f;
currentStability = Mathf.Clamp01(currentStability);
```

### 抖动实现

使用 **Perlin噪声** 生成平滑的抖动：

```csharp
float time = Time.time * baseTremorSpeed;
float noiseX = Mathf.PerlinNoise(time * noiseScaleX + noiseOffsetX, 0f) * 2f - 1f;
float noiseY = Mathf.PerlinNoise(0f, time * noiseScaleY + noiseOffsetY) * 2f - 1f;

// 抖动幅度 = 基础幅度 * (1 - 稳定性)
float tremorMultiplier = 1f - currentStability;
float currentAmount = Mathf.Lerp(baseTremorAmount, maxTremorAmount, tremorMultiplier);
```

### 瞄准漂移

准星会做 **8字形漂移**：

```csharp
float time = Time.time * aimDriftSpeed;
float driftX = Mathf.Sin(time * 1.1f) * currentDriftRadius;
float driftY = Mathf.Sin(time * 0.9f + 1.5f) * currentDriftRadius * 0.6f;
```

### 外部控制接口

```csharp
// 设置紧张程度（被瞄准、关键时刻）
tremorSystem.SetNervousness(0.8f);

// 设置疲劳程度（游戏时间越长）
tremorSystem.SetFatigue(0.5f);

// 设置地形影响
tremorSystem.SetTerrainEffect(0.4f);

// 蹲下提高稳定性
tremorSystem.OnStanceChanged(true);  // true = 蹲下

// 屏息（临时提高稳定性）
tremorSystem.OnBreathHold(true);
```

### 视觉反馈

- **准星大小**：抖动越大，准星越大
- **准星颜色**：绿→黄→红 表示稳定性
- **手部模型**：轻微晃动

---

## ⛳ 力度系统详解

### 高尔夫式蓄力

```
按住左键 → 力度条往返移动 → 松开发射
        ↑______________________↓
        0%        50%        100%
```

### 蓄力模式

```csharp
// 高尔夫式（默认）
private void UpdateGolfStyleCharging()
{
    if (isChargingForward)
    {
        currentChargeTime += Time.deltaTime * chargeSpeed;
        if (currentChargeTime >= maxChargeTime)
        {
            isChargingForward = false;  // 反向
        }
    }
    else
    {
        currentChargeTime -= Time.deltaTime * chargeSpeed;
        if (currentChargeTime <= 0f)
        {
            isChargingForward = true;   // 正向
        }
    }
}

// 普通模式（按住越久力度越大）
private void UpdateNormalCharging()
{
    currentChargeTime += Time.deltaTime * chargeSpeed;
    currentPower = Mathf.Clamp01(currentChargeTime / maxChargeTime);
}
```

### 力度计算

```csharp
// 最终力度 = 最小 + (最大 - 最小) * 力度百分比
float finalForce = Mathf.Lerp(minShootForce, maxShootForce, currentPower);

// 应用手抖偏移
Vector3 finalDirection = tremorSystem.GetAimDirectionWithTremor(aimDirection);

// 发射
rb.AddForce(finalDirection * finalForce, ForceMode.Impulse);
```

### 力度条UI

```
颜色渐变：
- 绿色 (0-33%)   : 轻力度
- 黄色 (34-66%)  : 中力度  
- 红色 (67-100%) : 重力度
```

---

## 🏔️ 地形系统详解

### 4种地形类型

| 地形 | 摩擦力 | 弹性 | 速度 | 特殊效果 |
|------|--------|------|------|----------|
| 水泥地 | 0.8x | 1.2x | 1.1x | 无 |
| 泥土地 | 1.3x | 0.7x | 0.9x | 坑洼、上坡 |
| 草堆 | 1.8x | 0.5x | 0.7x | 明显减速 |
| 松散土地 | 2.0x | 0.3x | 0.5x | 容易陷住 |

### 地形配置

```csharp
[System.Serializable]
public class TerrainEffectConfig
{
    public TerrainType terrainType;
    
    [Header("物理效果")]
    [Range(0f, 2f)] public float frictionMultiplier = 1f;      // 摩擦力倍率
    [Range(0f, 2f)] public float bouncinessMultiplier = 1f;    // 弹性倍率
    [Range(0f, 2f)] public float speedMultiplier = 1f;         // 速度倍率
    
    [Header("特殊效果")]
    public bool canStuck = false;                               // 是否会陷住
    [Range(0f, 1f)] public float stuckChance = 0f;             // 陷住概率
    public float stuckDuration = 2f;                            // 陷住持续时间
}
```

### 陷住效果

```csharp
private void ApplyStuckEffect(MarbleData marble, float duration)
{
    Rigidbody rb = marble.GetComponent<Rigidbody>();
    
    // 减速到几乎停止
    rb.linearVelocity *= 0.1f;
    rb.angularVelocity *= 0.1f;
    
    // 增加阻力
    rb.linearDamping = 10f;
    rb.angularDamping = 10f;
    
    // 延迟恢复
    StartCoroutine(RecoverFromStuck(rb, duration));
}
```

### 地形标签组件

```csharp
// 挂载在地形对象上
public class TerrainTag : MonoBehaviour
{
    public TerrainType terrainType = TerrainType.Cement;
    public Color gizmoColor = Color.white;  // 编辑器显示颜色
}
```

---

## 🎬 第一人称控制器

### 移动系统

```csharp
// 速度配置
walkSpeed = 3f;      // 行走
crouchSpeed = 1.5f;  // 蹲下
sprintSpeed = 5f;    // 冲刺
```

### 蹲下机制

```csharp
// 切换蹲下
if (Input.GetKeyDown(KeyCode.LeftControl) || Input.GetKeyDown(KeyCode.C))
{
    ToggleCrouch();
}

// 检查头顶空间
private bool CanCrouch()
{
    Vector3 checkPosition = transform.position + Vector3.up * standingHeight;
    return !Physics.CheckSphere(checkPosition, checkRadius, ceilingCheckLayer);
}
```

### 摄像机晃动

```csharp
// 移动时轻微晃动
if (isMoving)
{
    cameraBobTimer += Time.deltaTime * cameraBobFrequency;
    float bobOffset = Mathf.Sin(cameraBobTimer) * cameraBobAmplitude;
    cameraPos.y += bobOffset;
}
```

---

## 👤 第三人称模型

### 无需骨骼动画

使用简单几何体构建：

```csharp
// 自动创建模型
private void CreateDefaultModel()
{
    bodyRoot = CreateBodyPart("Body", ...);      // 身体
    head = CreateBodyPart("Head", ...);          // 头部
    leftArm = CreateBodyPart("LeftArm", ...);    // 左臂
    rightArm = CreateBodyPart("RightArm", ...);  // 右臂
    leftLeg = CreateBodyPart("LeftLeg", ...);    // 左腿
    rightLeg = CreateBodyPart("RightLeg", ...);  // 右腿
}
```

### 动画状态

```csharp
public enum AnimatorState
{
    Idle,       // 站立
    Walking,    // 行走
    Crouching,  // 蹲下
    Flicking    // 弹弹珠
}
```

### 弹弹珠动画

```
准备阶段 (0-60%):
- 手向后拉
- 身体略微后仰

释放阶段 (60-100%):
- 手快速弹出
- 身体回正
- 弹珠飞出
```

---

## 🖥️ Unity设置指南

### 快速开始（推荐）

1. 打开Unity项目
2. 点击菜单 **Dapaolou → Quick Setup**
3. 点击 **"一键创建场景"**
4. 运行游戏！

### 手动设置

#### 1. 创建Layer

```
Edit → Project Settings → Tags and Layers

添加Layer：
- Terrain (第8层)
- Marble (第9层)
- Player (第10层)
```

#### 2. 创建场景层级

```
场景层级：
├── --- 游戏管理 ---
│   ├── GameManager
│   │   └── GameManager.cs
│   ├── TerrainEffectSystem
│   │   └── TerrainEffectSystem.cs
│   └── GameCanvas
│       └── GameUIManager.cs
│
├── --- 玩家1 ---
│   ├── Player1 (PlayerManager.cs)
│   │   ├── CharacterController
│   │   ├── FirstPersonController.cs
│   │   ├── MarbleShooter.cs
│   │   ├── HandTremorSystem.cs
│   │   ├── Camera
│   │   └── ThirdPersonModel
│   │       └── ThirdPersonModel.cs
│   └── ...
│
├── --- 场景 ---
│   ├── Ground (地形)
│   │   ├── CementArea
│   │   ├── DirtArea
│   │   ├── GrassArea
│   │   └── SandArea
│   └── SpawnPoints
│       ├── Player1Spawn
│       ├── Player2Spawn
│       ├── Tower1Spawn
│       ├── Tower2Spawn
│       ├── Soldier1Spawn
│       └── Soldier2Spawn
│
└── --- 环境 ---
    └── 场景装饰物
```

#### 3. 创建弹珠预制体

```csharp
// 步骤：
1. 创建Sphere对象
2. 设置Scale为(0.05, 0.05, 0.05) - 5cm直径
3. 添加组件：
   - Rigidbody (mass: 0.1, collisionDetection: ContinuousDynamic)
   - SphereCollider
   - MarbleData
   - MarbleCollisionHandler
4. 创建PhysicMaterial:
   - Dynamic Friction: 0.4
   - Static Friction: 0.4
   - Bounciness: 0.3
5. 保存到 Assets/Prefabs/MarblePrefab.prefab
```

#### 4. 设置地形

```csharp
// 每个地形区域：
1. 创建Cube对象
2. 设置Scale（如 4, 0.01, 4）
3. 添加TerrainTag组件
4. 设置地形类型
5. 创建对应材质
6. 设置Layer为"Terrain"
```

#### 5. 配置GameManager

```csharp
// GameManager组件配置：
Player Count: 2
Player Colors: [蓝色, 红色]
Tower Spawn Points: [Tower1Spawn, Tower2Spawn]
Soldier Spawn Points: [Soldier1Spawn, Soldier2Spawn]
```

#### 6. 设置时间系统

```csharp
// 创建步骤：
1. 创建空对象，命名为TimeSystem
2. 添加TimeSystem脚本
3. 配置参数：
   - Time Scale: 60 (1秒=游戏内1分钟)
   - Start Hour: 6
   - End Hour: 20
4. 设置光照：
   - Directional Light: 拖拽场景中的主光源
   - 创建光照颜色渐变（6个时间点）
   - 创建光照强度曲线
5. 设置天空盒（可选）
```

#### 7. 设置NPC

##### 妈妈NPC
```csharp
1. 创建角色模型（或使用简单几何体）
2. 添加组件：
   - NavMeshAgent
   - Animator
   - AudioSource
   - MomNPC
3. 配置位置：
   - Home Position: 家的位置
   - Work Position: 干活位置
   - Kitchen Position: 厨房位置
   - Cow Pen Position: 牛圈位置
   - Gate Position: 大门位置
4. 配置音效：
   - Call For Meal Sounds: 喊饭音效
   - Kick Sound: 踢人音效
5. 设置Layer为NPC
```

##### 牛NPC
```csharp
1. 创建牛模型
2. 添加组件：
   - NavMeshAgent
   - Animator
   - AudioSource
   - CowNPC
3. 配置位置：
   - Pen Position: 牛圈位置
   - Outside Position: 院子外位置
   - Water Position: 水桶位置
4. 配置音效：
   - Moos Sounds: 牛叫声
5. 设置Layer为NPC
```

##### 狗NPC（臭豆）
```csharp
1. 创建狗模型
2. 添加组件：
   - NavMeshAgent
   - Animator
   - AudioSource
   - DogNPC
3. 配置位置：
   - Dog House Position: 狗窝位置
   - Tie Position: 拴绳位置
   - Wander Points: 闲逛点数组
   - Watch Position: 观看位置
4. 配置行为：
   - Escape Chance: 0.1 (10%概率逃跑)
   - Wander Duration: 60秒
   - Watch Duration: 30秒
5. 配置音效：
   - Bark Sounds: 叫声
   - Whine Sounds: 呜咽声
   - Happy Sounds: 开心声
6. 设置Layer为NPC
```

##### 猫NPC（骚咪）
```csharp
1. 创建猫模型
2. 添加组件：
   - NavMeshAgent
   - Animator
   - AudioSource
   - CatNPC
3. 配置位置：
   - Rest Positions: 休息位置数组（台阶、土墙）
   - Wander Points: 闲逛点数组
   - Lie Down Positions: 躺下位置数组
4. 配置行为：
   - Wander Chance: 0.3
   - Lie Down Chance: 0.2
   - Disturb Chance: 0.15
   - Disturb Range: 3
5. 配置音效：
   - Meow Sounds: 猫叫声
   - Purr Sounds: 呼噜声
   - Hiss Sounds: 嘶嘶声
6. 设置Layer为NPC
```

#### 8. 设置NPC管理器
```csharp
1. 创建空对象，命名为NPCManager
2. 添加NPCManager脚本
3. 拖拽所有NPC到对应槽位：
   - Mom: MomNPC
   - Cow: CowNPC
   - Dog: DogNPC
   - Cat: CatNPC
4. 配置交互：
   - Interaction Range: 3
   - NPC Layer: NPC
   - Interact Key: E
```

#### 9. 设置任务系统
```csharp
1. 创建空对象，命名为TaskSystem
2. 添加TaskSystem脚本
3. 配置时间限制：
   - Water Fetch Time: 120秒
   - Cow Lead Time: 180秒
   - Meal Time: 300秒
```

#### 10. 创建UI

```csharp
// Canvas设置：
1. 创建Canvas
2. Canvas Scaler: Scale With Screen Size
3. 参考分辨率: 1920x1080

// UI元素：
- PlayerInfoPanel (左上角)
- PowerBarPanel (底部中央)
- TremorIndicator (准星位置)
- HintText (中央)
- GameOverPanel (中央，默认隐藏)
```

---

## 🔧 调试技巧

### 1. 测试弹珠物理

```csharp
// 在Console中执行
GameObject marble = GameObject.Find("Player1_Soldier_0");
Rigidbody rb = marble.GetComponent<Rigidbody>();
rb.AddForce(Vector3.forward * 10f, ForceMode.Impulse);
```

### 2. 测试手抖系统

```csharp
// 调整手抖参数
HandTremorSystem tremor = FindObjectOfType<HandTremorSystem>();
tremor.SetNervousness(0.8f);  // 高紧张度
tremor.SetFatigue(0.5f);      // 中等疲劳
tremor.SetTerrainEffect(0.4f); // 地形影响
```

### 3. 测试地形效果

```csharp
// 修改地形类型
TerrainTag tag = FindObjectOfType<TerrainTag>();
tag.terrainType = TerrainType.LooseSand;  // 改为松散土地
```

### 4. 测试游戏流程

```csharp
// 跳过回合
GameManager.Instance.EndCurrentTurn();

// 拆炮楼
GameManager.Instance.TryDismantleTower(0);

// 检查玩家状态
PlayerData player = GameManager.Instance.GetPlayer(0);
Debug.Log($"小兵: {player.GetAliveSoldierCount()}");
Debug.Log($"炮楼: {player.GetAliveTowerCount()}");
Debug.Log($"可拆: {player.CanDismantleTower()}");
```

### 5. 可视化调试

```csharp
// 启用Gizmos显示
// 在Scene视图中可以看到：
// - 弹珠碰撞范围
// - 炮楼结构
// - 地形区域
// - 手抖范围
```

---

## 🚀 扩展功能

### 1. 音效系统

```csharp
// 需要添加的音效：
- 弹珠碰撞音效（不同力度不同音量）
- 弹射音效
- 地形滚动音效
- 背景音乐
- UI音效

// 实现位置：
MarbleCollisionHandler.cs → PlayHitEffects()
MarbleShooter.cs → ReleaseShot()
TerrainEffectSystem.cs → ApplyTerrainEffect()
```

### 2. 特效系统

```csharp
// 需要添加的特效：
- 碰撞火花粒子
- 弹珠轨迹拖尾
- 地形交互粒子（泥土飞溅等）
- 炮楼破碎特效
- 胜利庆祝特效

// 实现方式：
- 创建粒子预制体
- 在对应位置实例化
- 设置自动销毁
```

### 3. 网络多人

```csharp
// 推荐方案：
- Mirror (免费，功能强大)
- Photon (付费，易用)

// 需要同步的状态：
- 弹珠位置和旋转
- 弹珠发射参数
- 回合状态
- 玩家数据

// 同步策略：
- 弹珠物理：服务器权威
- 回合逻辑：主机权威
- 预测回滚：客户端预测
```

### 4. AI对手

```csharp
// 简单AI：
- 随机瞄准偏移
- 随机力度选择
- 优先攻击炮楼

// 中等AI：
- 计算最佳瞄准点
- 考虑地形影响
- 评估风险收益

// 困难AI：
- 精确瞄准
- 力度控制
- 策略选择（攻击炮楼还是小兵）
```

### 5. 场景搭建工作流

```csharp
// AI生成 + 手动拼接流程：
1. 用笔画出房子轮廓
2. 使用AI生成工具（如Meshy, Tripo）生成3D模型
3. 导入Unity
4. 手动调整和拼接
5. 添加碰撞体
6. 设置地形标签

// 推荐工具：
- Meshy.ai - AI生成3D模型
- Tripo3D - AI生成3D模型
- Blender - 手动调整模型
```

### 6. 角色动画

```csharp
// 当前方案：简单几何体动画
// 优点：无需骨骼绑定
// 缺点：动画不够流畅

// 进阶方案：
1. 使用Mixamo获取免费角色和动画
2. 导入Unity
3. 替换ThirdPersonModel
4. 使用Animator控制动画状态

// 需要的动画：
- Idle（站立）
- Walk（行走）
- Crouch（蹲下）
- Aim（瞄准）
- Flick（弹弹珠）
```

---

## ❓ 常见问题

### Q: 弹珠穿过了地面？
**解决方案**：
1. 确保地面有Collider组件
2. 弹珠的Collision Detection设置为Continuous Dynamic
3. 地面Layer设置正确
4. 检查物理材质设置

### Q: 炮楼自动倒塌？
**解决方案**：
1. 调整TowerBuilder的Stability Force参数
2. 增加弹珠的Mass
3. 增加FixedJoint的Break Force
4. 检查是否有其他力干扰

### Q: 手抖效果不明显？
**解决方案**：
1. 检查HandTremorSystem的enableTremor是否开启
2. 调整tremorFactors参数
3. 确保没有其他脚本覆盖方向
4. 查看Console是否有错误

### Q: 力度条不显示？
**解决方案**：
1. 检查MarbleShooter的Power Slider引用
2. 确保Canvas激活
3. 检查Slider组件配置
4. 确认UI层级正确

### Q: 地形效果不生效？
**解决方案**：
1. 确保TerrainTag组件已添加
2. 检查Layer设置
3. 确认TerrainEffectSystem单例存在
4. 检查射线检测距离

### Q: 第三人称模型不显示？
**解决方案**：
1. 检查Renderer组件是否启用
2. 确认材质颜色不是透明
3. 检查摄像机位置
4. 确认模型层级

---

## 📅 开发路线图

### 阶段1：核心物理 ✅
- [x] 弹珠预制体
- [x] 物理材质
- [x] 弹射系统
- [x] 碰撞检测

### 阶段2：游戏规则 ✅
- [x] 炮楼结构
- [x] 回合系统
- [x] 打爆判定
- [x] 拆炮楼机制

### 阶段3：玩家控制 ✅
- [x] 第一人称控制器
- [x] 蹲下机制
- [x] 手部动画

### 阶段4：手抖系统 ✅
- [x] Perlin噪声抖动
- [x] 瞄准漂移
- [x] 多因素影响

### 阶段5：地形系统 ✅
- [x] 4种地形类型
- [x] 物理效果
- [x] 陷住机制

### 阶段6：场景与UI ✅
- [x] 基础UI
- [x] 快速设置工具

### 阶段7：角色系统 ✅
- [x] 角色配置系统
- [x] 三个兄弟属性差异化
- [x] 角色选择UI
- [x] 弹珠库存系统（每人21个）

### 阶段8：时间系统 ✅
- [x] 时间流逝系统
- [x] 光照变化
- [x] 时间事件触发

### 阶段8：NPC系统 ✅
- [x] NPC基类
- [x] 妈妈NPC（做饭、喊吃饭、踢人）
- [x] 牛NPC（喝水、牵牛）
- [x] 狗NPC（逃跑、闲逛、观看）
- [x] 猫NPC（闲逛、躺着、骚扰）
- [x] 弟弟NPC（性格、推脱机制）
- [x] NPC管理器
- [x] 妈妈派活系统

### 阶段9：任务系统 ✅
- [x] 任务管理
- [x] 任务计时
- [x] 任务超时处理

### 阶段10：美术资源 ⏳
- [ ] 低多边形模型
- [ ] 材质纹理
- [ ] 环境装饰

### 阶段11：音效特效 ⏳
- [ ] 碰撞音效
- [ ] NPC音效
- [ ] 粒子特效
- [ ] 背景音乐

### 阶段12：网络多人 ⏳
- [ ] 选择网络方案
- [ ] 状态同步
- [ ] 延迟处理

### 阶段13：AI系统 ⏳
- [ ] 简单AI
- [ ] 难度级别
- [ ] 策略选择

---

## 📚 参考资源

### Unity文档
- [Physics](https://docs.unity3d.com/Manual/PhysicsSection.html)
- [CharacterController](https://docs.unity3d.com/ScriptReference/CharacterController.html)
- [Rigidbody](https://docs.unity3d.com/ScriptReference/Rigidbody.html)

### 手抖系统参考
- 《荒野的召唤》(theHunter: Call of the Wild)
- 《逃离塔科夫》(Escape from Tarkov)

### 力度系统参考
- 《高尔夫大师》(Golf Master)
- 《台球大师》(Pool Master)

### 3D模型资源
- [Mixamo](https://www.mixamo.com/) - 免费角色和动画
- [Kenney](https://kenney.nl/) - 免费游戏资源
- [Poly Pizza](https://poly.pizza/) - 低多边形模型

---

## 🎮 快速开始代码示例

### 创建弹珠并发射

```csharp
// 1. 获取玩家管理器
PlayerManager player = FindObjectOfType<PlayerManager>();

// 2. 获取可用弹珠
List<MarbleData> marbles = GameManager.Instance.GetCurrentPlayer().GetAvailableMarbles();

// 3. 选择弹珠
if (marbles.Count > 0)
{
    MarbleShooter shooter = player.GetComponent<MarbleShooter>();
    shooter.SelectMarble(marbles[0]);
}

// 4. 等待玩家操作（瞄准、蓄力、发射）

// 5. 监听发射事件
shooter.OnMarbleShot += (marble, direction, force) =>
{
    Debug.Log($"弹珠发射！力度: {force}");
};
```

### 处理碰撞

```csharp
// 在MarbleCollisionHandler中
private void HandleCollision(MarbleData attacker, MarbleData victim, float force)
{
    // 通知GameManager
    GameManager.Instance.OnMarbleCollision(attacker, victim, force);
}

// 在GameManager中
public void OnMarbleCollision(MarbleData attacker, MarbleData victim, float force)
{
    if (victim.marbleType == MarbleType.Tower)
    {
        HandleTowerHit(attackerPlayer, victimPlayer, victim, force);
    }
    else
    {
        HandleSoldierHit(attackerPlayer, victimPlayer, victim, force);
    }
}
```

### 控制手抖

```csharp
// 获取手抖系统
HandTremorSystem tremor = player.GetComponent<HandTremorSystem>();

// 瞄准时增加紧张度
tremor.SetNervousness(0.5f);

// 蹲下提高稳定性
tremor.OnStanceChanged(true);

// 获取当前抖动强度
float intensity = tremor.GetCurrentTremorIntensity();
Debug.Log($"当前手抖强度: {intensity}");
```

---

## 📝 更新日志

### v1.0.0 (2024)
- 初始版本
- 核心物理系统
- 游戏规则系统
- 手抖系统
- 地形系统
- 第一人称控制器
- 第三人称模型
- 基础UI
- 快速设置工具

### v2.0.0 (2024)
- 时间系统
  - 游戏内时间流逝（6:00-20:00）
  - 光照变化系统
  - 时间事件触发
- NPC系统
  - 妈妈NPC（做饭、喊吃饭、踢人）
  - 牛NPC（喝水、牵牛）
  - 狗NPC-臭豆（逃跑、闲逛、观看）
  - 猫NPC-骚咪（闲逛、躺着、骚扰）
  - NPC管理器
- 任务系统
  - 任务管理
  - 任务计时
  - 任务超时处理

---

## 👥 贡献指南

欢迎提交Issue和Pull Request！

### 如何贡献
1. Fork项目
2. 创建功能分支
3. 提交更改
4. 发起Pull Request

### 代码规范
- 使用C#命名规范
- 添加注释
- 保持代码整洁

---

## 📄 许可证

MIT License

---

**祝开发顺利！** 🎮

如有问题，请查阅本文档或提交Issue。
